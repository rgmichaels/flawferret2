{
  "name": "@flawferret2/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:release": "node build.mjs --bump-version",
    "check": "pnpm typecheck",
    "clean": "rm -rf dist",
    "dev": "node build.mjs --watch",
    "lint": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
